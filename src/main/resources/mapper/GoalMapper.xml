<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.z.finance.tracker.mapper.GoalMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO savings_goals (user_id, name, icon, target_amount, current_amount, deadline, created_at)
        VALUES (#{userId}, #{name}, #{icon}, #{targetAmount}, #{currentAmount}, #{deadline}, #{createdAt})
    </insert>

    <select id="findByUserId" resultType="com.z.finance.tracker.entity.SavingsGoal">
        SELECT * FROM savings_goals
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <select id="findById" resultType="com.z.finance.tracker.entity.SavingsGoal">
        SELECT * FROM savings_goals WHERE id = #{id}
    </select>

    <update id="update">
        UPDATE savings_goals
        SET name = #{name},
        icon = #{icon},
        target_amount = #{targetAmount},
        deadline = #{deadline}
        WHERE id = #{id} AND user_id = #{userId}
    </update>

    <update id="addAmount">
        UPDATE savings_goals
        SET current_amount = LEAST(current_amount + #{amount}, target_amount)
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM savings_goals WHERE id = #{id} AND user_id = #{userId}
    </delete>

</mapper>